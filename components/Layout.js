import React from 'react'
import NextHead from 'next/head'
import {
  node,
  string
} from 'prop-types'

const Layout = (props) => {
  return (
    <div className="wrap">
      
    </dvi>
  )
}

Layout.propTypes = {
  children: node
}

export default Layout